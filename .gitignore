# Database files
*.db
*.db-shm
*.db-wal

# Cache files
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg
*.egg-info/
dist/
build/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Local data
data/*.db
data/*.db-shm
data/*.db-wal
data/akshare_cache/
data/cache/
data/vector_bt_cache/
logs/
reports/
.kiro/
# test/ 目录保留测试文件
# doc/ 目录保留用于存放修复和文档，不忽略

# Environment
.env
.env.local
*.log

# Configuration files (local)
# 【2026-09-13】config/config.yaml 仍受版本管理（远端保留脱敏版，便于新克隆直接可用），
# 但本地工作区版本含飞书 App Secret 等真实凭据 → 【禁止提交该文件】。
# 已配置 .git/hooks/pre-commit 防护，误暂存时提交会被阻止。
# 需要修改远端默认配置请改 config/config.yaml.template 并同步脱敏内容。
config/tushare_config.json
config/87659999.json
config/87659999_decrypted.json

# 备份文件
*.bak

# 媒体文件（产品介绍、演示视频等）
*.mp4
*.pptx

# 根目录临时测试/验证脚本
_*.py
_*.txt
check_*.py
verify_*.py

# 临时输出文件
_tmp_*
_verify_output*
_output*.txt
_step_reinit.py

# 回测批次运行时文件（自动生成的临时批处理文件）
data/backtest_batch/progress_batch_*
data/backtest_batch/queue_batch_*
data/backtest_batch/single_*
data/backtest_batch/daily_*
data/backtest_batch/reports_*
data/backtest_batch/backtest_kline_*

# 验证临时数据
data/_verify_*

# 自动生成的交易脚本
trading/ptrade/test_khunter_auto_trade.py
trading/strategy_runner_exdividend_fix.py

# 运行时缓存
data/risk_cache/
data/trading_calendar_cache.json

# running 目录的备份副本
data/running - 副本*/

# PPT 和演示文稿生成
ppt/
scripts/generate_ppt.py
scripts/generate_video.py

# 根目录一次性运行脚本
quick_scan_*.py
remove_*.py
run_golden_*.py
scan_*.py
calc_temp_risk_*.py

# 临时测试脚本（非正式测试）
test/calc_temp_risk_*.py
test/check_*.py
test/verify_*.py

# 文档中的自动生成文件
doc/*.html
doc/video/

# running 目录的备份文件
data/running/*.json.bak
data/running/buy_candidate_pool*.json.bak*

# running 目录运行时产出数据（每日生成，不纳入版本控制）
data/running/daily_*.md
data/running/portfolio_*.json
data/running/signals_*.json
data/running/trades_*.json
data/running/task_history.json
data/running/last_exdividend_date.json
data/running/ptrade_feedback/
data/running/KHunter*.md
data/running/reduce_plan_cache.json
data/running/reduce_plan_cache.json.lock

# 根目录其他一次性脚本
fix_*.py
full_fix_*.py
golden_triangle_*.csv

# config 本地配置
config/backtest_engine_config.yaml

# running 运行时输出目录
data/running/to_ptrade/

# 测试用的音频/媒体文件
data/test_tts*.mp3

# 根目录更多一次性脚本
delete_stocks.py
detect_exdividend_*.py

# ===== 2026-09-12 推送脱敏：远端仅保留源代码，以下运行数据/产物不再入库 =====
# 说明：这些路径此前虽已写入本文件，但文件早已被跟踪，gitignore 对已跟踪文件无效；
#       本次配合 git-filter-repo 清理历史后，新产生的文件将被正确忽略。
# 运行数据（实时状态与批次产物）
data/backtest_batch/
data/running/
data/performance/
data/bk/
data/hmm_model/
data/*.sqlite
# 测试/文档产物
test/results/
test/*.csv
test/*.txt
doc/*.csv
# 本地推送备份目录（方案B执行用，成功后可删除）
_push_backup/
# 其他产物（2026-09-12 补充）
.coverage
*.sqlite-shm
*.sqlite-wal
*.bak_*
tmp_*.py

# ===== 2026-09-12 远端仅保留源码：以下内容已从版本历史剔除，不再跟踪 =====
# 注意：文件仍保留在本地磁盘，只是不纳入版本管理、不会被推送。
# 测试 / 文档 / 需求说明书
test/
doc/
spec/
# 本地辅助目录与发布包
.trae/
start.zip
# 根目录一次性脚本（调试/查询/验证用，不入库）
/debug_*.py
/diagnose_*.py
/find_*.py
/query_*.py
/list_*.py
/reinit_*.py
/restore_*.py
/verify_*.py
/test_*.py
