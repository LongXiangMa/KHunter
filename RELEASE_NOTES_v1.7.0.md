# KHunter v1.7.0 发版说明

- **版本号**：v1.7.0
- **发布日期**：2026-09-20
- **发布类型**：数据基础 + 稳定性修复
- **发布状态**：本地迭代（上游仓库 ling-0729/KHunter 无推送权限）

---

## 一、为什么做这次迭代

对 19 个选股策略做向量化对比回测后，得到两条关键结论：

1. **策略表现对区间极敏感**。同一策略在不同年份的超额收益从 -47% 跳到 +135%，
   单一区间得出的"最优策略"在样本外不成立。
2. **现有数据撑不起结论**。库内全市场覆盖实际从 2023-08 才开始（约 750 个交易日），
   做市场状态分层时最小的状态格子只有 39 天，样本量远远不够。

根因：`utils/stock_data_fetcher` 对无数据的股票默认只请求 365 天，
导致 5190 只股票中有 **5113 只的历史长度仅 500~1000 行**（约 2-4 年）。

---

## 二、新增工具

### 1. `tools/backfill_history.py` —— 历史 K 线补全

用 akshare 新浪接口（前复权）补齐 2015 年以来的日线。

**安全性**：`stock_kline` 表有 `UNIQUE(code, date)` 约束，脚本只执行
`INSERT OR IGNORE` —— **不修改、不删除任何现有数据**，重叠日期以库中已有值为准。

**口径对齐**（这是拼接多数据源最容易出问题的地方，已实测验证）：

| 项目 | 现有数据（TickFlow） | 新浪接口 | 处理方式 |
|---|---|---|---|
| 价格 | 前复权 | 前复权 | 重叠区间收盘价差异中位数 **0.029%**、最近日期 **0.0000%**，直接拼接 |
| 成交量 | 单位「手」 | 单位「股」 | 写入前统一 **÷100**，实测比例恒为 100.00 |

**工程特性**：多进程抓取（默认 6）、断点续传（`data/.backfill_progress.json`）、
退市股识别（新浪不再提供，单独归类不记为失败）、北交所自动跳过、
批量事务写入（SQLite WAL，可与 Web 服务同时运行）。

```bash
python tools/backfill_history.py --start 2015-01-01 --workers 6
python tools/backfill_history.py --limit 50        # 小样本试跑
python tools/backfill_history.py --verify-only     # 只做覆盖率体检
```

### 2. `tools/build_trading_calendar.py` —— 交易日历缓存生成

**修复的问题**：`utils/trade_date_utils.is_trading_day()` 在缺少
`data/trading_calendar_cache.json` 且没有 Tushare token 时直接抛错，导致：

* 选股时"非交易日自动回退到上一个交易日"失效 → 回退到原日期 → 当日无 K 线 → **0 只股票**；
* 股票池、回测等所有依赖交易日的功能一并失败。

本脚本用 akshare 生成该缓存（8797 个交易日，1990-12-19 ~ 2026-12-31），
**无需 Tushare token**，让系统在无 token 环境下也能正确判断交易日。

```bash
python tools/build_trading_calendar.py
python tools/build_trading_calendar.py --check    # 检查缓存是否过期
```

> 该缓存文件被 `.gitignore` 忽略（属于运行时产物），换环境或跨年后重新执行即可。

---

## 三、数据成果

2026-09-20 完成全量补全，耗时 **39.4 分钟**（6 进程并行）：

| 指标 | 补全前 | 补全后 |
|---|---|---|
| `stock_kline` 总行数 | 3,869,288 | **10,997,134** |
| 日均覆盖起始 | 2023-08（此前每年仅几十只） | **2015-01-05** |
| 历史 1500 行以上的股票 | 0 只 | **3659 只** |
| 历史 500-1000 行的股票 | 5113 只 | 525 只 |

**年度覆盖**（每年最后一个交易日的股票数）：

| 年份 | 2015 | 2016 | 2017 | 2018 | 2019 | 2020 | 2021 | 2022 | 2023 | 2024 | 2025 | 2026 |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 股票数 | 2337 | 2620 | 3037 | 3319 | 3525 | 3942 | 4455 | 4811 | 5029 | 5060 | 5137 | 5190 |

**执行结果**：成功 5177 只，退市 177 只（数据源已下架），失败 15 只（0.3%，
akshare 对个别代码解析异常），北交所 0 只（新浪不提供）。

**拼接质量抽检**：新旧数据交界（2023-08-17 附近）价格连续，无跳空；
重叠区间收盘价差异中位数 0.029%、最近日期 0.0000%。

**这次补全解锁了什么**：回测区间从 3 年扩展到 **11 年（2015 年至今，约 2700 个交易日）**，
足以支撑市场状态分层、walk-forward 稳健性检验与跨周期策略相关性分析 ——
此前这三项都因样本量不足而无法得出结论。

---

## 四、已知限制

- **北交所**（代码 8xxxxx / 4xxxxx）新浪接口不提供，未纳入补全范围；
- **退市股**无法补齐历史（数据源已下架），其历史仅保留库中原有部分 —— 这意味着
  2015-2022 年的回测存在**存活偏差**（只有活到今天的股票有数据），早期年份的
  实际股票数应多于表中的 2337 只，做长周期回测结论时须考虑这一点；
- 15 只股票（占 0.3%）因 akshare 对个别代码解析异常未补全；
- 新补数据来自新浪前复权，与原有 TickFlow 数据在**极早期日期的复权基准**存在
  0.03%~0.08% 的微小差异，不影响形态判定，但做精确收益率统计时需留意。

---

## 五、后续计划

数据补齐后，可开展此前因样本不足而无法进行的分析：

1. 市场状态分层（regime）与样本外验证；
2. Walk-forward 参数稳健性检验；
3. 更长区间的策略相关性分析，构建组合而非单策略。
