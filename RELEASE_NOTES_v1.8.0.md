# KHunter v1.8.0 发版说明

- **版本号**：v1.8.0
- **发布日期**：2026-09-21
- **发布类型**：功能新增 —— 量化验证模块

---

## 一、这次迭代做了什么

把此前在独立工具 [khunter-vectorbt](https://github.com/LongXiangMa/khunter-vectorbt) 中
验证过的能力**全部内置到 KHunter**，Web 界面新增「量化验证」页面，无需外部工具即可使用。

### 解决的痛点

KHunter 自带的回测是逐日事件驱动：每个交易日重新从数据库取全市场 5000+ 只股票
（实测 **5.19 秒/交易日**），19 个策略跑一年要 6.7 小时。新模块把信号一次性算好并缓存，
同一区间**重复回测只需几十秒**，适合横向比较多个策略。

---

## 二、新增功能

### 1. 市场状态开关（活跃度）

用全市场涨停家数占比的 5 日均值，与过去 250 个交易日的中位数比较，判断当前是否适合开新仓。

11 年（2015-2026）回测结论：**涨停横盘策略在高活跃状态年化超额 +27.5%，
低活跃状态仅 -3.8%**；样本外（2021-2026，规则定于 2015-2020）验证通过 ——
年化 11.45% → 24.42%、信息比率 0.59 → 1.01、最大回撤 -65.3% → -37.5%。

### 2. 策略对比回测（向量化）

一次跑多个策略并输出排名：累计收益 / 年化 / 基准年化 / 超额年化 / 信息比率 /
夏普 / 最大回撤 / 交易笔数 / 逐笔胜率，附净值曲线图。

可叠加状态过滤（不过滤 / 滚动阈值 / 固定阈值）。

### 3. 参数扫描（异步）

固定信号、扫描 止损 × 止盈 × 持有期（默认 4×4×3 = 48 组），
异步执行、可离开页面后回来查看结果。**关键用途是看参数敏感度** ——
同一策略 48 组结果的极差越大，越可能是过拟合。

---

## 三、代码结构

```
vector_bt/                     # 新增包（从 khunter-vectorbt 移植）
├── paths.py       路径常量（KHunter 根目录 / 数据库 / 缓存目录）
├── data.py        数据层：一次性载入 K 线并分组
├── signals.py     信号层：复用 strategy/*.py，指标只算一次 + 滚动切片
├── engine.py      向量化组合回测（T+1、涨跌停、分项费用、止损止盈路径依赖）
├── benchmark.py   基准指数（沪深300）抓取与缓存
├── regime.py      市场状态分层与开仓过滤器
├── runner.py      并行调度与对比入口
├── routes.py      Flask 蓝图 /api/vector
└── cli.py         命令行（与 Web 端等价，便于脚本化）

web/static/js/vector_bt.js     # 前端页面逻辑
web/templates/index.html       # 新增导航项「🧪 量化验证」与页面
web_server.py                  # 注册蓝图（第 219 行附近）
```

### 接口一览

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/vector/strategies` | 可选策略清单 |
| GET | `/api/vector/regime` | 最新交易日的活跃度状态与开仓建议 |
| POST | `/api/vector/backtest` | 多策略向量化回测（同步） |
| POST | `/api/vector/sweep` | 参数扫描（异步，返回 task_id） |
| GET | `/api/vector/task/<id>` | 异步任务状态查询 |

### 命令行（等价能力）

```powershell
cd D:\chatgpt\KHunter
python -m vector_bt regime-today                    # 今日状态与开仓建议
python -m vector_bt compare --strategy all --start 2021-01-04 --end 2026-09-18 --regime-filter rolling
```

---

## 四、缓存与数据

- 缓存目录：`data/vector_bt_cache/`（已在 `.gitignore` 中排除）
- 信号缓存键包含：策略 + 区间 + 股票池指纹 + 窗口参数，避免误用
- 11 年区间的信号缓存约 295 MB，命中后回测仅需数十秒

---

## 五、依赖

无新增依赖：复用 KHunter 已有的 pandas / numpy / Flask / akshare。

---

## 六、实测

| 操作 | 耗时 |
|---|---|
| 界面回测（涨停横盘，2021-2026，命中缓存） | **46 秒** |
| 同一区间无缓存时生成信号 | 约 1 小时（仅首次） |
| 对照：KHunter 原事件驱动回测同区间 | 约 3-5 小时 |

---

## 七、已知限制

- **首次运行某区间需要生成信号**（全市场 × 全区间），耗时较长；界面已提示，
  建议先用较短的区间试跑。
- 向量化引擎与 KHunter 原有事件驱动回测**不是逐笔等价**：本模块用日线 close-to-close
  计算收益、以等权持有近似；原有引擎有完整的资金占用与逐笔撮合模拟。
  两者适合不同用途：**横向对比用本模块，精细评估用原引擎**。
- 存活偏差：退市股无历史数据（数据源已下架）。
