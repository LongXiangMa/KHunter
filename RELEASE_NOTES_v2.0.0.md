# KHunter v2.0.0 发版说明

- **版本号**：v2.0.0
- **发布日期**：2026-09-21
- **发布类型**：功能新增 —— 选股策略与择时策略结合

---

## 一、为什么做这次迭代

用户提问：「有选股策略跟择时策略，你有没有结合两者进行」。

**答案是没有** —— 此前的向量化回测只用了选股策略（`strategy/`），
退场靠固定的持有期 + 止损止盈，KHunter 的 6 个择时策略
（`trading/timing_strategies.py`）完全没参与。这是能力缺口。

---

## 二、结合方式

新增 `vector_bt/timing.py`，把择时策略接入向量化引擎：

1. **进场过滤**：选股给出候选后，只有择时策略也给出买点（`is_buy`）才建仓；
2. **提前离场**：持仓期间若择时给出卖点（`is_sell`），当日收盘提前平仓
   （在止损/止盈/持有到期之外）。

评估口径与真实管线一致：传**降序**数据、`use_prev_day_signal=True`、
`position=None`（空仓视角）。

### 关键设计：按需评估而不是全量矩阵

全量评估择时要 5000 只 × 2800 天（约 1 小时，与一个选股策略同量级），
但实际只需要两类点：

* 进场：选股命中的 (股票, 日期) —— 一年几百个
* 离场：持仓期间的 (股票, 日期) —— 每笔约 10 天

因此 `TimingEvaluator` 改为按需评估 + 每股指标只算一次，**成本从小时级降到秒级**。

---

## 三、实测（涨停横盘策略，2021-01 ~ 2026-09）

| 配置 | 累计收益 | 年化 | 超额年化 | 信息比率 | **最大回撤** | 交易笔数 | 逐笔胜率 |
|---|---|---|---|---|---|---|---|
| 无择时 | 107.91% | 13.69% | +16.39% | 0.67 | -53.14% | 1136 | 42.25% |
| 海龟 | 77.12% | 10.54% | +13.24% | 0.52 | -51.55% | 631 | 43.11% |
| **布林带** | **116.63%** | **14.52%** | **+17.21%** | 0.63 | **-41.44%** | 205 | 41.95% |
| 支撑位 | -7.23% | -1.31% | +1.39% | -0.13 | -39.95% | 155 | **52.90%** |
| RSI | -34.47% | -7.14% | -4.45% | -0.24 | -82.60% | 578 | 39.97% |

**结论：布林带择时是最佳组合** —— 收益略高于无择时（14.52% vs 13.69%），
**回撤从 -53.14% 收敛到 -41.44%**，交易笔数减少 82%（1136 → 205）。

支撑位择时的逐笔胜率最高（52.9%）但交易太少（155 笔），收益无法覆盖。
RSI 择时显著有害（回撤放大到 -82.6%）。

---

## 四、使用方法

```powershell
cd D:\chatgpt\KHunter
python -m vector_bt compare --strategy "涨停横盘策略" `
    --start 2021-01-04 --end 2026-09-18 `
    --signal-range 2015-01-01 2026-09-18 --window 120 `
    --timing bollinger --save
```

`--timing` 可选：`turtle` / `low_turtle` / `support` / `rsi` / `bollinger`
（顺势宝 `macd_bollinger` 需要授权文件，未纳入）。

---

## 五、踩坑记录

**症状**：RSI / 布林带 / 支撑位三种择时全区间 **0 笔交易**。

**根因**：这几个策略以 `calculate_indicators(df, stock_code=...)` 调用（带关键字参数），
而指标预计算优化里的替换函数写成 `lambda df: df` —— 只收一个参数，
调用即抛 `TypeError`，又被异常兜底静默吞掉，表现为"全程无信号"。

**修复**：替换函数改为 `lambda df, *args, **kwargs: df`。

**教训**：适配器里的异常兜底必须留下计数与样例（本模块已有 `errors`/`error_samples`），
否则"静默无信号"极难定位。

---

## 六、已知限制

- 离场用的是"空仓视角"的卖点（`position=None`），未模拟择时策略的加仓/减仓分支；
  原引擎有完整持仓状态，精确评估请用原引擎
- 择时买点与选股信号取交集，可能显著减少交易机会（布林带从 1136 降到 205 笔），
  需自行权衡样本量与信号质量
